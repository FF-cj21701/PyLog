# --- Shared Constants and Styles ---
AXIS_WIDTH = 0  # We hide axes lines, but might need space? 

from ..utils.plot_value_utils import INVALID_VALUE_SENTINELS

# Industry standard null mnemonics for log data
NULL_MNEMONICS = list(INVALID_VALUE_SENTINELS)
